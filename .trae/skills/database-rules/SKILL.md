---
name: database-rules
description: Use to follow SQLite/DAO/Service Layer rules—never access DB directly from React
---

SQLite

DAO Pattern

Service Layer

Never access database from React.

Electron Main Process handles database.

Renderer uses IPC only.